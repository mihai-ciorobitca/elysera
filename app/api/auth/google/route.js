import {cookies} from 'next/headers'
import {referralInput,referralCookie} from '@/lib/auth/referral-policy.mjs'
import {NextResponse} from 'next/server'
import {authClient} from '@/lib/auth/server'
import {sameOrigin,authConfig} from '@/lib/auth/policy.mjs'
import {oauthOrigin} from '@/lib/auth/google-policy.mjs'
import {loginRateLimit} from '@/lib/auth/rate-limit'
export async function POST(request){const reply=(b,s=200)=>NextResponse.json(b,{status:s,headers:{'Cache-Control':'no-store'}});if(!sameOrigin(request))return reply({error:'Nicht zulässig.'},403);try{const raw=await request.text();if(raw.length>2048)return reply({error:'Ungültige Eingabe.'},400);if(raw){let body;try{body=JSON.parse(raw)}catch{return reply({error:'Ungültige Eingabe.'},400)}if(body?.referral){const code=referralInput(body.referral);if(!code)return reply({error:'Bitte einen gültigen ELYSERA Referral-Link oder Empfehlungscode eingeben.'},400);(await cookies()).set(referralCookie,code,{httpOnly:true,sameSite:'lax',secure:new URL(request.url).protocol==='https:',path:'/',maxAge:60*60*24*30})}}if(!await loginRateLimit(request,'','google'))return reply({error:'Zu viele Versuche. Bitte später erneut versuchen.'},429);const origin=oauthOrigin(request),client=await authClient();const {data,error}=await client.auth.signInWithOAuth({provider:'google',options:{redirectTo:origin+'/auth/google/callback',skipBrowserRedirect:true,queryParams:{prompt:'select_account'}}});if(error||!data.url||new URL(data.url).origin!==new URL(authConfig().url).origin)return reply({error:'Google-Anmeldung vorübergehend nicht verfügbar.'},503);return reply({url:data.url})}catch{return reply({error:'Google-Anmeldung vorübergehend nicht verfügbar.'},503)}}
