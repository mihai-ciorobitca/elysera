export async function merchandisingHttp(request,write,{db,currentAdmin,sameOrigin,reply,read,mutate}){if(write&&!sameOrigin(request))return reply({error:'Diese Anfrage ist nicht zulässig.'},403);try{const actor=await currentAdmin();if(!actor)return reply({error:'Administrator-Anmeldung erforderlich.',code:'AUTH_REQUIRED'},401);if(!write)return reply({actorId:actor.id,state:await read(db)});const text=await request.text();if(text.length>6000)return reply({error:'Eingabe zu lang.'},400);let raw;try{raw=JSON.parse(text)}catch{return reply({error:'Ungültige Eingabe.'},400)}return reply(await mutate(db,actor,raw))}catch(e){return reply({error:e.status?e.message:'Ergebnis nicht bestätigt. Dieselbe Anfrage erneut versuchen.',code:e.status?e.code:'UNCONFIRMED'},e.status||503)}}
